// import { fromJS } from 'immutable';
// import { makeSelectSuccessPageDomain } from '../selectors';

// const selector = makeSelectSuccessPageDomain();

describe('makeSelectSuccessPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
